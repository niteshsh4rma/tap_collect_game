name: tap_collect_game
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flame: ^1.21.0
  flame_bloc: ^1.12.3
  flutter_bloc: ^8.1.6
  equatable: ^2.0.7
  flame_audio: ^2.10.6
  flame_lottie: ^0.4.2+3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/audio/
    - assets/lottie/
